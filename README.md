Урок 1: Введение в язык C и основы Git/GitHub
Основы систем контроля версий: Git и GitHub

Контроль версий (Version Control) – это практика, позволяющая отслеживать изменения в файлах проекта и при необходимости возвращаться к предыдущим состояниям. Пример из жизни – история изменений в Google Docs: можно увидеть все правки и откатиться к более ранней версии документа
dan-it.com.ua
. В программировании для этих целей создана система контроля версий Git. Git – это распределённая система управления версиями, изначально разработанная Линусом Торвальдсом для работы над ядром Linux
gist.github.com
. Ключевые возможности Git: сохранение всех изменений каждого файла, возможность восстановить любой предыдущий вариант и параллельная работа нескольких разработчиков над проектом без потери данных
dan-it.com.ua
dan-it.com.ua
.

GitHub, в свою очередь, – это онлайн-платформа, основанная на Git, для хранения репозиториев и совместной работы над ними. Проще говоря, Git – это инструмент для версии файлов на вашем компьютере, а GitHub – удалённый сервис (социальная код-платформа) для обмена этими файлами через интернет
dan-it.com.ua
practicum.yandex.ru
. Благодаря GitHub разработчики могут выкладывать свой код в открытый доступ, привлекать сторонних участников и совместно разрабатывать проекты из любой точки мира. Основные отличия между Git и GitHub можно свести к нескольким пунктам:

Локально vs Онлайн: Git хранит историю изменений локально (на вашем ПК), а GitHub – на удалённом сервере (в интернете), доступном всем подключённым участникам
dan-it.com.ua
.

Отслеживание изменений: Git позволяет отслеживать версии файлов и переключаться между ними локально. GitHub же визуализирует эту историю онлайн и предоставляет инструменты для обзора кода, обсуждений и pull request (предложений изменений).

Сотрудничество: Git сам по себе не предоставляет механизмов общения, а GitHub встроенно поддерживает совместную разработку: управление доступом, отзывы к коду, трекер задач и др.
dan-it.com.ua
dan-it.com.ua
.

Базовый рабочий цикл с Git/GitHub: Работа с Git обычно строится вокруг нескольких основных команд (их выполняют в терминале или через интерфейс IDE):

Клонирование репозитория: чтобы получить удалённый проект себе на компьютер, используют команду git clone <URL> – она создаёт локальную копию всего репозитория с его историей изменений
gist.github.com
.

Добавление изменений: после редактирования файлов нужно подготовить изменения к сохранению. Команда git add <имя_файла> добавляет файл в индекс (стадию подготовки)
dan-it.com.ua
. Можно указать . вместо имени файла, чтобы добавить все изменённые файлы.

Фиксация изменений (коммит): команда git commit -m "сообщение" создаёт новый снимок состояния проекта (коммит) с указанным описанием изменений
dan-it.com.ua
. Каждый коммит – как сохранённая версия проекта, позволяющая в будущем понять, что было сделано (по сообщению) и при необходимости вернуться к нему.

Отправка на GitHub: командой git push изменения из локального репозитория отправляются в удалённый (на сервер GitHub)
dan-it.com.ua
. Обычно пишут git push origin main, где origin – имя удалённого репозитория, а main – ветка (основная линия разработки).

Получение обновлений: чтобы подтянуть изменения, которые появились в удалённом репозитории (например, преподаватель выложил новые материалы), используют git pull. Эта команда забирает свежие коммиты с сервера и сливает их с вашими локальными изменениями
gist.github.com
. После git pull локальная копия проекта обновится до актуального состояния.

💡 Примечание: В реальной работе с Git есть и другие команды (ветвление, слияние и пр.), но выше перечислены самые необходимые для начала. В контексте нашего курса GitHub будет использоваться для получения учебных материалов и сдачи домашних заданий. Убедитесь, что у вас установлен Git и создан аккаунт на GitHub. Вы можете воспользоваться встроенной интеграцией Git в IDE CLion или выполнять команды в терминале.

Структура простой программы на C

Перед тем как изучать переменные и ввод/вывод, рассмотрим минимальную структуру программы на языке C. Любая Си-программа состоит из функций, и главной функцией является main(), с неё начинается выполнение. Также для ввода-вывода понадобится подключить стандартную библиотеку. Вот пример классической первой программы – “Hello, World!”:

#include <stdio.h>   // подключаем заголовочный файл для ввода-вывода

int main(void) {
    printf("Hello, World!\n");  // вывод строки на экран
    return 0;                   // возвращаем 0, обозначая успешное завершение
}


Разберём её по частям. Первая строка #include <stdio.h> – это директива препроцессору включить заголовочный файл stdio.h (Standard Input/Output header)
ru.wikibooks.org
. Этот файл содержит объявления функций для ввода и вывода, таких как printf и scanf, которыми мы будем пользоваться.

Далее идет определение функции main. В примере она объявлена как int main(void). Это означает, что функция main не принимает аргументов (void) и возвращает целое число (тип int)
ru.wikibooks.org
. Возврат значения 0 (return 0;) принято интерпретировать как код успешного завершения программы.

В теле функции мы вызываем printf("Hello, World!\n");. Функция printf печатает текст, переданный ей в кавычках, на экран (в консоль). Символ \n означает перевод строки (новая строка). Таким образом, программа при запуске выведет на экран:

Hello, World!


После выполнения printf, программа доходит до return 0; и заканчивает свою работу. Эта простая программа демонстрирует базовый синтаксис C: директивы #include, определение функции main и вызов функции из стандартной библиотеки.

☝️ Совет: В CLion (или другой IDE) вы можете создать новый проект (консольное приложение на C), и среда автоматически сгенерирует шаблон, похожий на приведённый код. Попробуйте запустить программу с выводом "Hello, World!" в вашей среде разработки, чтобы убедиться, что всё настроено правильно.

Переменные и типы данных в C

Переменная – это именованная область памяти, в которой хранится некоторое значение. У каждой переменной в C обязательно есть тип данных, определяющий множество допустимых значений и операций, которые можно над этой переменной выполнять. Проще говоря, тип данных характеризует объем памяти, отведённый под данные, их внутреннее представление, допустимый диапазон значений и набор доступных операций
victor192007.narod.ru
. Поэтому при объявлении переменной нужно указать её тип.

В языке C имеются несколько основных категорий простых (скалярных) типов данных:

Целые числа: тип int – основной целочисленный тип (как правило, 4 байта). Также существуют модификации: меньший short (обычно 2 байта) и больший long (4 или 8 байт в зависимости от системы), а для очень больших целых – long long (обычно 8 байт). По умолчанию целые знаковые (могут быть положительными или отрицательными). Например, тип int на большинстве систем занимает 4 байта (32 бита), что даёт диапазон примерно от –2,147,483,648 до 2,147,483,647
victor192007.narod.ru
. Тип char тоже относится к целым: он хранит код символа в 1 байте (8 бит). Символьный тип способен представлять числовые коды символов примерно от 0 до 255 (либо –128…127, если char считается знаковым)
victor192007.narod.ru
.

Числа с плавающей запятой (десятичные дроби): тип float – для вещественных чисел одиночной точности (обычно занимает 4 байта, ~7 значащих цифр точности), и double – двойной точности (8 байт, ~15 значащих цифр)
victor192007.narod.ru
victor192007.narod.ru
. Эти типы предназначены для хранения дробных чисел (например, 3.14, -0.001) с определённой точностью. В стандарте C также есть long double (ещё более высокой точности, но на современных PC часто совпадает по размеру с double).

Логический тип: В языке C исторически не было отдельного булевого типа до стандарта C99. Начиная с C99, можно подключить заголовок <stdbool.h> и использовать тип _Bool (или алиас bool) для хранения логических значений true (истина) или false (ложь). Однако, часто в C используют целые int для этих целей (0 как false, не-ноль как true).

При объявлении переменной указывается тип, а затем её имя. Например: int count; объявляет целочисленную переменную с именем count. Ей сразу же выделяется место в памяти (например, 4 байта для int). До присвоения значения такая переменная имеет неопределённое содержимое (остатки данных, что лежали в этой области памяти). Использование неинициализированной переменной ведёт к непредсказуемым результатам – фактически, переменная содержит "мусор"
pvs-studio.ru
. Поэтому важно сразу инициализировать переменные, либо присваивать им значения перед первым использованием.

Примеры объявления и инициализации разных типов переменных:

int age = 18;          // целочисленная переменная age, сразу инициализирована значением 18
float price = 99.99f;  // переменная с плавающей точкой, суффикс f обозначает литерал типа float
char grade = 'A';      // символьная переменная, хранит код символа 'A' (например, 65 в ASCII)


Обратите внимание на синтаксис: числовые литералы по умолчанию рассматриваются как int (для целых) или double (для дробных). Суффикс f в 99.99f явно задаёт литерал типа float. Символьные литералы пишутся в одиночных кавычках (например, 'A'). Также есть строковые литералы в двойных кавычках – о них поговорим позднее (строка в C – это массив символов, оканчивающийся специальным нулевым байтом).

После объявления переменные можно использовать в выражениях и выводить. Пример:

int a = 5, b = 7;
int sum = a + b;    // сложение целых
printf("Сумма %d + %d = %d\n", a, b, sum);


Здесь %d – спецификатор формата для вывода целого числа (о форматированном выводе подробнее далее). Код напечатает: Сумма 5 + 7 = 12.

Можно запросить у компилятора размер типа или переменной с помощью оператора sizeof. Например, sizeof(int) вернёт размер типа int в байтах на данной системе, а sizeof age – размер конкретной переменной age. Если вывести эти значения, то убедимся, что на распространённых архитектурах char занимает 1 байт, int и float – по 4 байта, а double – 8 байт
younglinux.info
victor192007.narod.ru
. Интересная демонстрация: напишите программу, которая выводит размеры основных типов данных, например:

printf("sizeof(char) = %zu байт\n", sizeof(char));
printf("sizeof(int) = %zu байт\n", sizeof(int));
printf("sizeof(float) = %zu байт\n", sizeof(float));
printf("sizeof(double) = %zu байт\n", sizeof(double));


Запустив её, вы увидите “вес” (объём памяти) каждого типа. Этот опыт показывает, что типы занимают разный объём памяти, что влияет на диапазон представимых значений. Например, double будучи вдвое больше float по размеру, может представлять значительно более широкие диапазоны чисел
victor192007.narod.ru
.

Важное замечание: диапазоны и размеры типов формально зависят от конкретного компилятора и архитектуры. Стандарт C гарантирует минимальные допустимые пределы (например, int не меньше 16 бит). Но на практике почти на всех современных системах: char = 1 байт, short = 2 байта, int = 4 байта, long = 4 или 8 байт, long long = 8 байт, float = 4 байта, double = 8 байт. Зная это, можно приблизительно понимать границы: например, 4-байтовый unsigned int охватывает 0 до ~4,29 млрд
learn.microsoft.com
, а 1-байтовый char – от 0 до 255 в беззнаковом варианте
learn.microsoft.com
.

Ввод и вывод данных: функции printf и scanf

Для взаимодействия программы с пользователем (или вообще внешним миром) служат функции вывода и ввода. Мы уже видели пример вывода через printf. Рассмотрим её подробнее, а затем перейдём к вводу через scanf. Обе функции определены в подключённом заголовке <stdio.h>.

Форматированный вывод: printf

Функция printf позволяет выводить форматированные данные на стандартный вывод (обычно консоль). Она принимает строку формата и перечень переменных (или литералов), которые нужно вставить в эту строку вместо специальных спецификаторов формата. Спецификатор начинается с символа % и обозначает место и способ вывода следующего аргумента. Например:

int x = 42;
printf("Значение x = %d\n", x);


Спецификатор %d означает вывести целое число (формат decimal). В результате на экран будет напечатано: Значение x = 42. Если нужно вывести несколько значений разных типов, перечисляем их через запятую, а в строке формата указываем соответствующие спецификаторы в порядке следования. Пример:

int a = 3;
const char* animal1 = "собаки";
int b = 2;
const char* animal2 = "коты";
printf("%d %s, %d %s.\n", a, animal1, b, animal2);


Вывод: 3 собаки, 2 коты. Здесь %d подставляет числа, а %s – строки (строковый литерал или char*). Важно: количество и тип спецификаторов должны строго соответствовать числу и типам переданных аргументов, иначе результат непредсказуем.

Часто используемые спецификаторы формата для printf():

%d – для вывода int (десятичное целое число)
younglinux.info
.

%ld – для long (длинное целое), %hd – для short. Также %lld для long long.

%u – для unsigned int (неотрицательное целое).

%c – для вывода одного символа (char).

%s – для вывода строки (массива char, оканчивающегося '\0').

%f – для вывода числа с плавающей точкой (типа float или double). По умолчанию выводит 6 знаков после точки.

%e или %g – для научного (экспоненциального) формата вещественных.

%lf – для double в некоторых случаях можно явно указать (но обычный %f тоже работает, так как printf ожидает double при передаче float благодаря promotion).

%p – для вывода указателя (адреса в памяти).

Можно также управлять форматом вывода: например, %.2f выведет вещественное число с 2 знаками после запятой, а %5d выведет целое в поле ширины 5 символов (подравнивая по правому краю). Пример: printf("%.2f\n", 3.14159); напечатает 3.14.

Форматированный ввод: scanf

Для ввода данных из стандартного потока (клавиатуры) используется функция scanf. Её синтаксис подобен printf, но работает она наоборот: она считывает текст, введённый пользователем, интерпретирует его согласно формату и помещает результаты в переменные. Общая форма вызова:

scanf("формат", адрес_переменной1, адрес_переменной2, ...);


Обратите внимание: в scanf нужно передавать адреса переменных, куда записывать считанные значения
younglinux.info
. Это логично, ведь функция должна изменить значение переменной, находящейся вне её собственного тела. Чтобы получить доступ к переменной, она должна знать, куда (в какую ячейку памяти) записывать – поэтому нужен адрес. Операндом для получения адреса является амперсанд &. Например, если у нас int age;, то вызов будет scanf("%d", &age);. Исключение – для строк (массивов char): при чтении строки scanf("%s", str); передавать & не нужно, потому что имя массива и так неявно преобразуется в адрес его первого элемента
younglinux.info
.

Спецификаторы формата для scanf сходны с printf, с небольшими отличиями:

%d – считывает десятичное целое (int)
codelessons.dev
.

%f – считывает число с плавающей точкой (float). Для типа double используется %lf (long float)
younglinux.info
.

%c – считывает один символ (char). Важно: scanf("%c", &ch) прочитает любой символ, в том числе пробел или перевод строки.

%s – считывает строку (набор символов до первого пробела) в массив char[].

Есть и другие: %i (целое число, распознает основание системы счисления), %u (unsigned), %o/%x для восьмеричных/шестнадцатеричных, и т.д.
codelessons.dev
codelessons.dev
.

Пример простого ввода:

int a;
float b;
char c;
printf("Введите целое, вещественное и символ:\n");
scanf("%d %f %c", &a, &b, &c);
printf("Вы ввели: %d, %.2f, %c\n", a, b, c);


Здесь в формате scanf спецификаторы разделены пробелами. scanf по умолчанию пропускает любые пробельные символы (пробел, перевод строки, табуляцию) перед попыткой чтения очередного значения, поэтому мы можем вводить данные через пробел или с новой строки – не важно. Например, ввод может выглядеть так в одной строке: 10 3.14 A, либо на отдельных строках. После выполнения, переменные a, b, c получат соответственно 10, 3.14 и 'A'. Функция вернёт число успешно прочитанных значений (в данном случае 3).

Важное замечание про чтение символов: когда вы используете %c для чтения символа, scanf не пропускает пробелы автоматически перед символом (в отличие от %d, %f и %s). Поэтому часто бывает, что предшествующий перевод строки \n или пробел остаётся в буфере ввода и сразу же читается как символ. Чтобы этого избежать, можно поставить пробел перед %c в строке формата: например, scanf(" %c", &c); – такой формат сначала пропустит whitespace, а затем прочитает символ. Либо использовать другие функции вроде getchar(). В первом уроке достаточно знать, что при чтении нескольких значений разных типов в одной строке формата, как мы сделали, проблем обычно не возникает (мы специально записали %c сразу после %f без пробела, чтобы игнорируемый пробел не засчитался как символ
younglinux.info
). Для простоты можно сначала практиковаться без чтения отдельных символов.

Пример: прочитаем два целых числа от пользователя и выведем их сумму. Это совместит использование scanf, операций над переменными и printf:

int x, y;
printf("Введите два целых числа:\n");
scanf("%d %d", &x, &y);
int result = x + y;
printf("Сумма чисел %d и %d равна %d\n", x, y, result);


Пользователю будет предложено ввести два числа (например, 4 и 7). После ввода и нажатия Enter, программа посчитает сумму и выведет, например: Сумма чисел 4 и 7 равна 11.

Основные арифметические операции

В C, как и в большинстве языков, доступны стандартные арифметические операторы для чисел:

+ – сложение (также унарный плюс, обозначающий знак числа, обычно не используется явно).

- – вычитание (и унарный минус для смены знака числа).

* – умножение.

/ – деление.

% – остаток от деления (modulo). Оператор % применим только к целым числам (int и др.).

Операторы * и / имеют более высокий приоритет, чем + и - (как умножение и деление в математике выполняются до сложения/вычитания). Можно использовать скобки ( ), чтобы явно задавать нужный порядок вычислений.

Целочисленное vs. вещественное деление: Будьте внимательны при делении чисел. Если оба операнда – целые, то результат деления тоже будет целым с отбрасыванием дробной части. То есть, 7 / 4 даст 1, а не 1.75, поскольку остаток отбрасывается
ru.stackoverflow.com
. Это встроенное поведение C (и C++): при делении целых всегда получается целое (математический результат усекается до целой части). Если хотя бы один операнд – вещественный (float/double), выполняется вещественное деление и дробная часть сохраняется. Например, 7.0 / 4 или 7 / 4.0f дадут 1.75. Таким образом, чтобы получить нецелый результат, нужно либо делать операнды вещественными изначально, либо приводить тип явно. Пример:

int a = 7, b = 4;
printf("%d / %d = %d\n", a, b, a/b);        // 7/4 = 1 (целочисленное деление)
printf("%d / %d = %.2f\n", a, b, a/(float)b); // 7/4 = 1.75 (явно приводим b к float)


Вторая строка выводит 1.75, потому что мы привели b к float – в выражении a/(float)b переменная a тоже неявно преобразуется к float, и выполняется вещественное деление.

Оператор % (остаток) возвращает остаток от целочисленного деления. Например, 10 % 3 равняется 1 (так как 10 = 3*3 + 1), а 8 % 4 = 0 (4 делится на 8 без остатка). Этот оператор часто используют для задач, где нужно определить чётность (x % 2 == 0 означает число x чётное), или выделить часы/минуты из минут/секунд и т.п. Помните, что % не определён для вещественных типов в C – только для int и других целых (если нужны остатки для float, используются функции из <math.h> вроде fmod, но это за рамками основ).

Также в C есть сокращённые операторы присваивания (например, a += 5; эквивалентно a = a + 5;), инкремент a++ (увеличить на 1) и декремент a-- (уменьшить на 1). Эти конструкции могут облегчать запись, но их можно постепенно изучать. В первой задаче достаточно понимать обычные операции.

Теперь, когда мы рассмотрели переменные, операции и ввод/вывод, перейдём к практике – разберём несколько примеров программ, а затем предложим задачи для самостоятельного решения.

Примеры задач

Пример 1: Подсчёт суммы двух чисел.
Задача: Написать программу, которая запрашивает у пользователя два целых числа и выводит их сумму.
Решение: Используем тип int для хранения вводимых чисел. Чтение организуем через scanf, вывод результата – через printf с форматом для целых. Вот код решения:

#include <stdio.h>

int main(void) {
    int num1, num2;
    printf("Введите два целых числа: ");
    scanf("%d %d", &num1, &num2);
    int sum = num1 + num2;
    printf("Сумма %d и %d равна %d\n", num1, num2, sum);
    return 0;
}


Пример выполнения: если ввести числа 5 и 7, программа выведет Сумма 5 и 7 равна 12. Все просто – переменные num1 и num2 получают значения ввода, затем мы вычисляем сумму и выводим её. Обратите внимание, как в scanf разделены спецификаторы через пробел – это значит, пользователь может вводить числа через пробел или с новой строки.

Пример 2: Конвертер температуры (Цельсий -> Фаренгейт).
Задача: Написать программу, которая запрашивает температуру в градусах Цельсия и переводит её в градусы Фаренгейта. Формула перевода: F = C * 9/5 + 32.
Решение: Здесь удобнее использовать тип float или double, так как результат может быть дробным. Возьмём float для обеих величин. При расчёте важно использовать дробное деление 9.0/5.0, иначе при 9/5 (целые) получится 1 вместо 1.8. Код решения:

#include <stdio.h>

int main(void) {
    float celsius;
    printf("Введите температуру в градусах Цельсия: ");
    scanf("%f", &celsius);
    float fahrenheit = celsius * 9.0f / 5.0f + 32.0f;
    printf("%.1f°C = %.1f°F\n", celsius, fahrenheit);
    return 0;
}


Мы используем спецификатор %f для чтения и вывода float. Формат "%.1f°C = %.1f°F\n" выведет значение с одним знаком после запятой и добавит символы °C и °F для наглядности. Пример выполнения: при вводе 0 (ноль градусов Цельсия) программа рассчитает и покажет 0.0°C = 32.0°F. При вводе 100 (точка кипения воды) получим 100.0°C = 212.0°F.

В этом примере ещё раз подчеркнём нюанс: 9.0f/5.0f равно 1.8 (вещественное), тогда как будь это 9/5 (целые литеры), получилось бы 1. Хоть переменная celsius и типа float, если бы мы написали celsius * 9 / 5 + 32, операция 9/5 выполнилась бы первой в целых, дала бы 1, и итоговая формула стала бы celsius * 1 + 32, что неверно. Поэтому всегда следите за типами в выражениях.

Пример 3: Вычисление параметров прямоугольника.
Задача: Пользователь вводит две величины: ширину и высоту прямоугольника (целые числа). Программа должна рассчитать площадь и периметр этого прямоугольника и вывести результаты.
Решение: Площадь = width * height, периметр = 2*(width + height). Типы возьмём int (если предполагается целочисленные размеры). Код:

#include <stdio.h>

int main(void) {
    int width, height;
    printf("Введите ширину и высоту прямоугольника: ");
    scanf("%d %d", &width, &height);
    int area = width * height;
    int perimeter = 2 * (width + height);
    printf("Площадь = %d, Периметр = %d\n", area, perimeter);
    return 0;
}


Пример выполнения: ввод: 5 3 (ширина 5, высота 3). Вывод: Площадь = 15, Периметр = 16. Формат вывода можно при необходимости улучшить, например, добавить единицы измерения.

Эти примеры демонстрируют, как применять теорию на практике: мы объявляем переменные нужных типов, читаем входные данные, выполняем вычисления и выводим результаты.

Задачи для самостоятельной практики

Предлагаются несколько задач для решения. Попробуйте написать программы самостоятельно, используя материалы урока. Для каждой задачи указано, что требуется на ввод и какой должен быть вывод.

Приветствие пользователя. Запросите у пользователя его имя (одно слово, например, "Иван") и возраст (целое число). Затем выведите в консоль приветственное сообщение. Например, если введены имя Анна и возраст 20, программа может вывести: Привет, Анна! Вам 20 лет.. Используйте формат %s для имени и %d для возраста в printf. Учтите, что имя без пробелов можно считать через %s напрямую.

Периметр и площадь прямоугольника. (Модификация примера). Напишите программу, которая запрашивает два целых числа – ширину и высоту прямоугольника, – а затем рассчитывает и выводит его площадь и периметр. Формат вывода: например, для ввода 7 и 4 программа должна вывести Площадь = 28, Периметр = 22. Убедитесь, что формулы расчёта верны.

Конвертер температуры (Фаренгейт -> Цельсий). Напишите программу, которая запрашивает у пользователя температуру в градусах Фаренгейта и переводит её в градусы Цельсия. Формула: C = (F - 32) * 5/9. Для ввода и вывода используйте тип float. Проверьте на известных точках: 32°F должно давать 0°C, 212°F – 100°C. Обратите внимание на порядок операций: в формуле нужно сначала вычесть 32, затем умножить на 5, потом поделить на 9. Если в коде будете делить нацелое – получите неправильный результат (см. раздел о целочисленном делении). Постарайтесь это учесть (например, можно умножить на 5.0 и разделить на 9.0, или просто выполнить все операции в float).

Среднее из трех чисел. Запросите у пользователя три числа (можно вещественные) и выведите их среднее арифметическое. Например, ввод: 5 0 15 -> вывод: Среднее = 6.67. Для расчёта суммы и среднего используйте подходящий тип. В выводе рекомендуется форматировать результат с двумя знаками после запятой.

Дополнительный совет: после написания кода всегда тестируйте программу на нескольких наборах данных, включая граничные случаи (например, ноль, отрицательные значения), чтобы убедиться, что она работает правильно.

На этом первое занятие завершено. Мы познакомились с базовыми понятиями: переменные, типы данных, ввод-вывод в языке C, а также рассмотрели, как будет организовано взаимодействие через Git/GitHub. На следующем уроке продолжим расширять знания (условные операторы, циклы и т.д.). Успехов в решении задач!
