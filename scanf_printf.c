#include <stdio.h>

int main() {
    int x,y;
    printf("Enter two int numbers: ");
    scanf("%d %d", &x, &y);
    int result = x + y;
    printf("Result of %d and %d equils %d\n", x, y, result);

}